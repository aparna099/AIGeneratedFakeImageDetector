data_loader.py

This file provides functions for loading and preprocessing images from a directory.

Functions:
- load_images_from_directory(directory): Loads images and corresponding labels from the specified directory.
- preprocess_images(images): Preprocesses the loaded images.
- preprocess_labels(labels): Preprocesses the labels.
- split_data(images, labels): Splits the data into training and test sets.
